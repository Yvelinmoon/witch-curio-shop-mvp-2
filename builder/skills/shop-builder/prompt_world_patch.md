Patch the runtime config for the existing shop shell.

You may change:
- shop name
- world eyebrow text
- assistant display text
- theme color tokens
- intro / loading copy

Theme rules:
- Keep foreground and background colors visibly separated across panels, cards, tabs, buttons, dialogue bubbles, item details, library pages, toasts, and report popups.
- Use a dark, shop-appropriate backdrop for modal overlays; avoid bright overlay masks that wash out the page.
- Give the assistant / clerk area enough visual presence for portrait and reaction text, especially when the supply card area is short.

Keep unchanged:
- core merge board rules
- existing save format beyond additive metadata
